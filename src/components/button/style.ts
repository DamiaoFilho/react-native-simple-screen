import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    body: {
        borderRadius: 8,
        padding: 8,
        width: 125,
    },
    text: {
        textAlign: "center",
        fontWeight: "bold",
        color: "white"
    }
})